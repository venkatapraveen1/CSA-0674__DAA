def is_palindrome(x: int) -> bool:
    if x < 0:
        return False
    reversed_x = int(str(x)[::-1])
    return x == reversed_x

# Test the function
x = 121
print(is_palindrome(x))  # Output: True
